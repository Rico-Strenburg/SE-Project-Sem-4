class Pattern:
    def __init__(self, patternId, screenerId, name, must_match):
        self.patternId = patternId
        self.screenerId = screenerId
        self.name = name
        self.must_match = must_match